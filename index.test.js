describe("", () => {
  it("should create the first version if no tag found", () => {});
  it("should bump MINOR but not PATCH", () => {});
  it("should bump MINOR twice but not PATCH", () => {});
  it("should bump PATCH but not MINOR", () => {});
  it("should bump PATCH twice but not MINOR", () => {});
  it("should NOT bump PATCH or MINOR when MAJOR is bumped", () => {});
  it("should keep bumping release tag (release_*) regardless features or bug fixes", () => {});
  it("should recognise bugfix, feature with extra spaces on naming convention", () => {});
});
